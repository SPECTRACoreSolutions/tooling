# Tooling Docs

This folder holds per-tool references (CLI/API/endpoints/credentials flow). Current docs:
- `railway.md`, `railway-api.md`
- `remote-dev.md`, `termius.md`
- `fabric.md`, `azure.md`, `docker.md`
- `openai.md`
- `openai-cheatsheet.md`
- `package-distribution.md` (existing)

Add more per tool (e.g., Spectra CLI, GitHub Actions, etc.): include install/usage, CLI flags, API endpoints, auth/token handling, and where related secrets live (Railway vars, key vaults). Keep secrets out of docs; reference their storage location only.
